# [项目名] Design DNA

> source_url: [来源 URL，设计模式重新截图用]
> 提炼时间：[日期]

---

## 视觉个性

**色调基础**（截图直接观察，不要猜）：[亮色 / 暗色 / 中性] — 背景色 `[实际值]`

用 3-5 句话描述整体气质：
- 情绪基调：
- 复杂度：
- 目标受众感：
- 辨识度最强的特征（具体，如"负字间距标题 + 荧光黄 CTA"）：

**反模式**（这个设计绝对不会出现的东西）：
- [从截图观察得出]
- [从截图观察得出]

---

## 颜色系统

### 基础调色板

| 名称 | 值 | 用途 |
|------|-----|------|
| Background | `[实际值]` | 页面底色 |
| Surface | `[实际值]` | 卡片/面板底色 |
| Text Primary | `[实际值]` | 主要文字 |
| Text Secondary | `[实际值]` | 描述/副标题 |
| Text Tertiary | `[实际值]` | 占位符/元数据 |
| Border | `[实际值]` | 默认边框 |
| Border Hover | `[实际值]` | 悬停边框 |

### 品牌/强调色

| 名称 | 值 | 用途 |
|------|-----|------|
| Accent / Brand | `[实际值]` | 主要 CTA、高亮 |
| Accent Hover | `[实际值]` | 悬停态 |
| Success | `[实际值]` | 成功状态 |
| Error | `[实际值]` | 错误状态 |

### 渐变（如有）

```css
/* 如果有品牌渐变，记录在这里；没有则删掉此节 */
```

---

## 字体系统

### 字族

```css
--font-sans: '[字体名]', [fallback];
--font-mono: '[字体名]', monospace;  /* 如无代码字体需求可删除 */
```

CDN 引入（如有）：
```html
<!-- 从 <head> 实际观察到的 link 标签 -->
```

### 字号系统

| 名称 | 值 | 行高 | 字重 | 用途 |
|------|-----|------|------|------|
| H1 | `[实际值]` | `[实际值]` | `[实际值]` | 页面主标题 |
| H2 | `[实际值]` | `[实际值]` | `[实际值]` | 区块标题 |
| H3 | `[实际值]` | `[实际值]` | `[实际值]` | 子标题 |
| Body | `[实际值]` | `[实际值]` | `[实际值]` | 正文 |
| Small | `[实际值]` | `[实际值]` | `[实际值]` | 辅助文字 |

特殊处理（如 letter-spacing、font-feature-settings）：

---

## 间距系统

基准单位：`[实际值，通常 4px 或 8px]`

常用场景：
- 按钮 padding：`[实际值]`
- 卡片内 padding：`[实际值]`
- 区块垂直间距：`[实际值]`
- 栅格 gap：`[实际值]`

---

## 布局系统

```css
max-width: [实际值];
padding: 0 [实际值];
```

---

## 圆角系统

```css
--radius-sm: [实际值];   /* 标签、小元素 */
--radius-md: [实际值];   /* 按钮、输入框 */
--radius-lg: [实际值];   /* 卡片 */
```

---

## 阴影/层叠系统

```css
/* 记录实际使用的 shadow 或说明"用 border 代替 shadow" */
```

---

## 动效规范

```css
--duration: [实际值];
--easing:   [实际值];
```

---

## 组件词汇

### Primary Button
```css
background: [实际值];
color: [实际值];
padding: [实际值];
border-radius: [实际值];
/* hover 态： */
```

### Secondary Button
```css
background: [实际值];
color: [实际值];
border: [实际值];
/* hover 态： */
```

### 卡片
```css
background: [实际值];
border: [实际值];
border-radius: [实际值];
padding: [实际值];
```

### 其他特征组件（如有）
<!-- 记录这个设计独有的、其他设计没有的组件特征 -->

---

## 设计 Do & Don't

### ✅ Do
- [从截图观察得出的正例]

### ❌ Don't
- [从截图观察得出的反例]
